# macOS system files
.DS_Store

# Maven build output
/target/

# VS Code settings & workspace files
/.devcontainer/devcontainer.json.lock

# VS Code general settings (but we want to keep tasks.json)
/.vscode/*
!/.vscode/tasks.json

.env

# Used to trigger redeployment of .war, to have the exploded .war be mounted from our dev container
# to the tomcat server, so that we can remove its contents when rebuilding the container to trigger
# detection of changes of the .war
tomcat-webapps/