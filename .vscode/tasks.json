{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build & Hot-Deploy",
      "type": "shell",
      "command": "mkdir -p src/main/resources && cp .env src/main/resources/ && rm -rf target/webapps/ROOT && mvn clean package && rm -f src/main/resources/.env && cp target/cheeper.war tomcat-webapps/ROOT.war && echo '✅ Deployed new ROOT.war — http://localhost:8080/'",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
