// .vscode/tasks.json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build & Deploy",
      "type": "shell",
      "command": "mvn clean package",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}